import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF4CAF50);
  static const primaryDark = Color(0xFF388E3C);
  static const primaryLight = Color(0xFFC8E6C9);

  // Accent
  static const accent = Color(0xFFFF9800);
  static const accentDark = Color(0xFFF57C00);

  // Neutrals
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const divider = Color(0xFFBDBDBD);

  // Status colors
  static const statusRequested = Color(0xFF2196F3);
  static const statusAccepted = Color(0xFF9C27B0);
  static const statusInProgress = Color(0xFFFF9800);
  static const statusDelivered = Color(0xFF00BCD4);
  static const statusCompleted = Color(0xFF4CAF50);
  static const statusCancelled = Color(0xFFF44336);

  // Semantic
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
  static const warning = Color(0xFFFFA000);
  static const info = Color(0xFF1976D2);
}
